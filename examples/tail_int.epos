fn tail(lst: list(int), n: int = 1): list(int)
  empty_list: list(int) = {}
  result: list(int) = match n then
    len(lst) -> empty_list
    _ -> {elem(lst, n), ..tail(lst, n + 1)}
  end
  result
end

print(tail({1, 2, 3, 4})) # should return {2, 3, 4}
