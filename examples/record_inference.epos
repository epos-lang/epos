# Record inference
record person
  name: string
  age: int
end

sally: person = @{name => "Sally", age => 25}
bob := @{name => "Bob", age => 30}

fn get_name(p: person): string
  p.name
end

sally.get_name().print() # This works
bob.get_name().print() # This doesn't
