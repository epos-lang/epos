# Gets the tail of a list
fn tail*(lst: list(t), n: int = 1): list(t)
  empty-list: list(t) = {}
  result: list(t) = match n then
    len(lst) -> empty-list
    _ -> {lst |> elem(n), ..tail(lst, n + 1)}
  end
  result
end
