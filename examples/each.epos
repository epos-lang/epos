# Loops over each element in a list
# Example:
#   (1..3).each(fn(x: int) => print(x))
fn each*(lst: list(t), fun: fn(t), index: int = 0)
  lst
  .elem(index)
  .fun()
  match index < len(lst) - 1 then
    true => each(lst, fun, index + 1)
  end
end
